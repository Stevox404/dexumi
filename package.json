{
  "name": "mangapi",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "electron .",
    "electron:start": "cd electron && npm start",
    "react:start": "cd react && npm start",
    "react:build": "cd react && npm run build",
    "cors-proxy:start": "cd cors-proxy && npm start"
  },
  "dependencies": {
    "electron": "^21.1.0"
  }
}
